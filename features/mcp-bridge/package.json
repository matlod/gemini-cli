{
  "name": "@gemini-cli-mod/mcp-bridge",
  "version": "0.1.0",
  "description": "MCP bridge for Claude Code to communicate with Gemini CLI A2A server",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gemini-mcp-bridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.3",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
